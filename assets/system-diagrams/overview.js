/* The whole-system overview is a Swimlane only. The five major-process
 * sequences are authored in sequence-models.js and rendered separately. */
(function(){
'use strict';
window.SystemDiagramOverview={activity:id=>window.SystemPortraitSwimlane(id)};
})();
